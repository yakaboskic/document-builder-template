# Results <!-- \label{sec:results} -->

Put your findings here. Lean on variable substitution — if every
number in the paper comes from `manifest.yaml`, you update values in
one place and everything else stays consistent.

## Primary analysis

Our primary test rejected the null with $p = {{PVAL:.2e}}$ across
{{NUM_SAMPLES:,}} samples (see Methods, Section \ref{sec:methods}).

The effect was consistent across subgroups (Table \ref{tab:example}).
As shown in Figure \ref{fig:example}, the framework captures the
relationship we expected.

### Inline-markdown table

Use a plain markdown pipe table when the values won't change between
edits — captions, summary descriptors, presentation-only tables.

| Metric        | Value | Notes              |
|---------------|------:|--------------------|
| Sample size   | {{NUM_SAMPLES:,}} | Total enrolled |
| R<sup>2</sup>        | {{R2:.2f}}  | Primary regression |
| p-value       | {{PVAL:.2e}} | Two-sided, BH-adj |

### Data-driven table from `tables/subgroup_summary.csv`

Use a separate data file when the table is generated by an analysis
script. Declare it in `manifest.yaml` under `tables:`, then reference
with a `{{tab:<id>}}` token. The token expands to a markdown pipe
table at build time so PDF, HTML, and Word all render identically:

{{tab:subgroup_summary}}

Re-run your analysis, overwrite the CSV, rebuild — every output
stays in sync.

## Robustness

Follow-up analyses corroborated the primary finding. Replace this
paragraph with your own, or add subsections as needed.
